<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <h2>Edit Product</h2><br>
    <form action="/products/<%=product._id%>?_method=PUT" method="POST">
        <label for="name">Product name</label>
        <input type="text" name="name" id="name" placeholder="product name" value="<%=product.name%>">
        <br><label for="price">Price</label>
        <input type="number" name="price" id="price" placeholder="price" value="<%=product.price%>" >
        <br><label for="category">Select category</label>
        <select name="category" id="category">
            <% for(i in category){ %>+
                <option value="<%= i %>" <%= product.category === "<%= i %>" ? "selected" : "" %> ><%= i %></option>
            <% } %>    
            
            <!-- <option value="fruit" <%=product.category === "fruit" ? 'selected' : '' %>>fruit</option>
            <option value="vegetablem" <%=product.category === "vegetable" ? 'selected' : '' %>>vegetable</option>
            <option value="dairy" <%=product.category === "dairy" ? 'selected' : '' %>>dairy</option> -->
        
        </select>
    </form>
    <br><a href="/products/<%=product._id%>">Cancel</a>

</body>
</html>